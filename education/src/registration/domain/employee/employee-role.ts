/* eslint-disable prettier/prettier */

import { Common } from "../common";

export class EmployeeRole extends Common{
    id: string;
    isActive: boolean;
    roleName: string;
    employeeId: string;
    roleId: string;
}